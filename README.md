# Foreman has moved

This repo is archived. Foreman now lives in **[Pitcocy/skills](https://github.com/Pitcocy/skills)**,
alongside the rest of my agent skills, so there is one place to install from and one
place that stays current.

```bash
npx skills@latest add Pitcocy/skills --all -g --agent claude-code
```

Everything that was here came along: `foreman`, `write-a-prd`, `prd-to-plan`,
`grill-with-docs`, `execute-phase`, `tdd`, `prototype`, the worker templates, and the
project templates. The docs are at
[skills/foreman/README.md](https://github.com/Pitcocy/skills/blob/main/skills/foreman/README.md).

The code in this repo still works if you have it cloned, but it stopped getting updates
in July 2026. Point new installs at the new home.
